<%= form_for(@app_info, :html => { :multipart => true }) do |f| %>
  <% if @app_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_info.errors.count, "error") %> prohibited this app_info from being saved:</h2>

      <ul>
      <% @app_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :app_url %><br />
    <%= f.text_field :app_url %>
  </div>
	<div class="field">
		<%= f.file_field :image %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
